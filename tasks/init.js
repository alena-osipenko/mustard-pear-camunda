load ( EXT );
/**
 * Установка умолчаний
 * */
execution.setVariable ( 'culture', ext.es.get('culture') );
execution.setVariable ( 'region', ext.es.get('region') );
execution.setVariable ( 'kadastr', ext.es.get('cladr') );
execution.setVariable ( 'result', 0);

